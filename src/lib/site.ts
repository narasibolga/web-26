import { routing } from "@/i18n/routing";

export const SITE_URL = "https://narasibolga.com";

export const LOCALES = routing.locales;

export const OG_LOCALE_MAP: Record<string, string> = {
  en: "en_US",
  id: "id_ID",
};
